# Calendly (Meeting Scheduling Platform) - System Design Interview Answer

## Introduction

"Today I'll design a meeting scheduling platform like Calendly. The core problem is allowing users to share their availability and let others book meetings without the back-and-forth of email. This involves interesting challenges around calendar integration, time zones, and most critically - preventing double bookings. Let me walk through my approach."

---

## 🎯 Step 1: Requirements Clarification

### Functional Requirements

"Let me confirm the core functionality:

1. **Availability Management**: Users define their working hours and availability patterns
2. **Meeting Types**: Different meeting durations (15min, 30min, 60min) with buffer times
3. **Booking Flow**: Invitees see available slots and book instantly
4. **Calendar Integration**: Sync with Google Calendar, Outlook to check/block time
5. **Notifications**: Email confirmations, reminders, cancellation notices
6. **Time Zone Handling**: Display times correctly for users in different time zones

Should I also include group scheduling or team round-robin assignments?"

### Non-Functional Requirements

"For a scheduling platform:

- **Consistency**: Absolutely no double bookings - this is critical
- **Latency**: Availability checks should be fast (<200ms)
- **Availability**: 99.9% uptime - broken booking links hurt users
- **Scale**: 1 million users, 400K+ bookings per day
- **Calendar API Limits**: Must respect external API rate limits"

---

## 📊 Step 2: Scale Estimation

"Let me work through the numbers:

**Users & Bookings:**
- 1M active users
- Average 3 bookings per user per week
- Daily bookings: 1M × 3 / 7 = ~430K bookings/day
- Booking RPS: 430K / 86400 = ~5 RPS (50 RPS peak)

**Availability Checks:**
- Users browse ~100 slots before booking
- Availability check RPS: 430K × 100 / 86400 = ~500 RPS
- Peak (business hours): ~5,000 RPS

**Storage:**
- User data: 1M × 10KB = 10 GB
- Meeting types: 1M × 5 types × 5KB = 25 GB
- Bookings: 430K/day × 365 × 10KB = 1.5 TB/year
- Calendar cache: 1M × 100 events × 5KB = 500 GB

The write load is low, but availability calculations are read-heavy and compute-intensive."

---

## 🏗️ Step 3: High-Level Architecture

```
┌─────────────┐          ┌─────────────┐
│   Invitee   │          │    Host     │
│  (Browser)  │          │  (Browser)  │
└──────┬──────┘          └──────┬──────┘
       │                        │
       ▼                        ▼
┌─────────────────────────────────────────┐
│         Load Balancer (nginx)           │
└─────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────┐
│      API Gateway / Application Layer    │
│          (Node.js + Express)            │
└─────────────────────────────────────────┘
                    │
     ┌──────────────┼──────────────┬──────────────┐
     ▼              ▼              ▼              ▼
┌──────────┐  ┌────────────┐ ┌────────────┐ ┌──────────────┐
│ Booking  │  │Availability│ │Integration │ │Notification  │
│ Service  │  │ Service    │ │ Service    │ │ Service      │
└──────────┘  └────────────┘ └────────────┘ └──────────────┘
     │              │              │              │
     └──────────────┼──────────────┴──────────────┘
                    │
     ┌──────────────┼──────────────┐
     ▼              ▼              ▼
┌──────────┐  ┌────────────┐  ┌────────────┐
│PostgreSQL│  │   Redis    │  │  RabbitMQ  │
│(Primary) │  │  (Cache)   │  │  (Queue)   │
└──────────┘  └────────────┘  └────────────┘
                                     │
                                     ▼
                          ┌────────────────────┐
                          │ External Calendar  │
                          │ APIs (Google, MS)  │
                          └────────────────────┘
```

---

## 🔧 Step 4: Core Service Design

### Booking Service

"This is the critical path - where double-booking prevention happens.

**Booking Flow:**

```
┌─────────────────────────────────────────────────────────────────┐
│                      BOOKING FLOW                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  1. Invitee selects time slot                                   │
│     │                                                           │
│     ▼                                                           │
│  2. Booking Service validates slot is still available           │
│     │                                                           │
│     ▼                                                           │
│  3. Acquires lock on host's calendar for that time              │
│     │                                                           │
│     ▼                                                           │
│  4. Creates booking record in database                          │
│     │                                                           │
│     ▼                                                           │
│  5. Creates external calendar event (async if API slow)         │
│     │                                                           │
│     ▼                                                           │
│  6. Releases lock                                               │
│     │                                                           │
│     ▼                                                           │
│  7. Triggers confirmation emails via queue                      │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**Preventing Double Bookings (The Hard Part):**

I'd use a multi-layered approach:

```
┌─────────────────────────────────────────────────────────────────┐
│               THREE-LAYER BOOKING PROTECTION                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  LAYER 1: Database Constraints                                  │
│  ─────────────────────────────                                  │
│  PostgreSQL exclusion constraint using GiST index               │
│  Prevents overlapping time ranges for same host                 │
│  Key: (host_user_id, tsrange(start_time, end_time))            │
│  This is the LAST LINE OF DEFENSE - catches bugs               │
│                                                                 │
│  LAYER 2: Row-Level Locking                                     │
│  ──────────────────────────                                     │
│  Within transaction: SELECT ... FOR UPDATE on host's row        │
│  Serializes all booking attempts for same host                  │
│  Then check for conflicts before inserting                      │
│  Prevents race conditions during booking creation               │
│                                                                 │
│  LAYER 3: Optimistic Locking                                    │
│  ─────────────────────────                                      │
│  Version field on bookings table                                │
│  UPDATE with WHERE version = expected_version                   │
│  If 0 rows affected, concurrent modification occurred           │
│  Handles modifications to existing bookings                     │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**Why all three layers?**
- Constraints: Last line of defense, catches bugs
- Row locking: Prevents race conditions during booking creation
- Optimistic locking: Handles concurrent modifications to existing bookings"

### Availability Service

"This calculates available time slots by merging multiple data sources.

```
┌─────────────────────────────────────────────────────────────────┐
│              AVAILABILITY CALCULATION ALGORITHM                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Input: host_id, date, meeting_duration                         │
│                                                                 │
│  Step 1: Get host's availability rules (working hours)          │
│          ──▶ e.g., Mon-Fri 9am-5pm in host's timezone          │
│                                                                 │
│  Step 2: Get existing bookings from our database                │
│          ──▶ All confirmed bookings for that date              │
│                                                                 │
│  Step 3: Get external calendar events (from cache or API)       │
│          ──▶ Google Calendar, Outlook events                   │
│                                                                 │
│  Step 4: Merge all busy periods                                 │
│          ──▶ Sort by start time, merge overlapping intervals   │
│                                                                 │
│  Step 5: Find gaps that fit meeting duration + buffer           │
│          ──▶ Walk through merged busy periods                  │
│          ──▶ Collect gaps >= duration + buffer_before/after    │
│                                                                 │
│  Output: List of available time slots                           │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**Period Merging Algorithm:**

```
┌─────────────────────────────────────────────────────────────────┐
│                    INTERVAL MERGING                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Input (unsorted):                                              │
│  ┌─────┐  ┌─────────┐    ┌───┐ ┌───────┐                       │
│  │ 9-10│  │ 9:30-11 │    │2-3│ │3:30-5 │                       │
│  └─────┘  └─────────┘    └───┘ └───────┘                       │
│                                                                 │
│  Algorithm:                                                     │
│  1. Sort by start time                                          │
│  2. For each period:                                            │
│     - If overlaps with last merged ──▶ Extend end time         │
│     - Else ──▶ Add as new period                               │
│                                                                 │
│  Output:                                                        │
│  ┌──────────────┐      ┌───┐ ┌───────┐                         │
│  │    9-11      │      │2-3│ │3:30-5 │                         │
│  └──────────────┘      └───┘ └───────┘                         │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**Caching Strategy:**
- Cache computed availability for 5 minutes
- Cache key pattern: availability:{host_id}:{date}:{meeting_type_id}
- Invalidate on: new booking, booking cancellation, calendar sync"

### Integration Service

"Handles OAuth flows and calendar synchronization.

**Sync Strategy (Hybrid Push/Pull):**

```
┌─────────────────────────────────────────────────────────────────┐
│                  CALENDAR SYNC STRATEGY                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────┐      ┌─────────────────────┐          │
│  │ PUSH (Webhooks)     │      │ PULL (Polling)      │          │
│  │ ─────────────────── │      │ ───────────────────  │          │
│  │ Google Calendar     │      │ Every 10 minutes    │          │
│  │ sends notification  │      │ for stale records   │          │
│  │ to our endpoint     │      │ or webhook failures │          │
│  └──────────┬──────────┘      └──────────┬──────────┘          │
│             │                            │                      │
│             ▼                            ▼                      │
│  ┌───────────────────────────────────────────────────┐         │
│  │              Sync Handler                          │         │
│  │ 1. Fetch events from provider                     │         │
│  │ 2. Cache events (10-min TTL)                      │         │
│  │ 3. Invalidate availability cache                  │         │
│  └───────────────────────────────────────────────────┘         │
│                                                                 │
│  ON-DEMAND (Fallback):                                         │
│  When user views availability, trigger fresh sync              │
│  Only if cache is older than 10 minutes                        │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**Rate Limit Handling:**
- On RateLimitError: Back off and retry later (start at 60 seconds)
- On TokenExpiredError: Refresh OAuth token and retry
- Circuit breaker pattern: If provider is consistently failing, fall back to cached data"

---

## 💾 Step 5: Data Model

### Core Tables

```
┌─────────────────────────────────────────────────────────────────┐
│                          USERS                                   │
├─────────────────────────────────────────────────────────────────┤
│  id (UUID PK)                                                   │
│  email (VARCHAR UNIQUE NOT NULL)                                │
│  name (VARCHAR NOT NULL)                                        │
│  time_zone (VARCHAR NOT NULL)  ──▶ e.g., 'America/New_York'    │
│  created_at (TIMESTAMP)                                         │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                      MEETING_TYPES                               │
├─────────────────────────────────────────────────────────────────┤
│  id (UUID PK)                                                   │
│  user_id (UUID FK → users)                                      │
│  name (VARCHAR NOT NULL)                                        │
│  duration_minutes (INTEGER NOT NULL)                            │
│  buffer_before_minutes (INTEGER DEFAULT 0)                      │
│  buffer_after_minutes (INTEGER DEFAULT 0)                       │
│  is_active (BOOLEAN DEFAULT true)                               │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                   AVAILABILITY_RULES                             │
├─────────────────────────────────────────────────────────────────┤
│  id (UUID PK)                                                   │
│  user_id (UUID FK → users)                                      │
│  day_of_week (INTEGER NOT NULL)  ──▶ 0=Sunday, 6=Saturday      │
│  start_time (TIME NOT NULL)                                     │
│  end_time (TIME NOT NULL)                                       │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                        BOOKINGS                                  │
├─────────────────────────────────────────────────────────────────┤
│  id (UUID PK)                                                   │
│  meeting_type_id (UUID FK → meeting_types)                      │
│  host_user_id (UUID FK → users)                                 │
│  invitee_email (VARCHAR NOT NULL)                               │
│  invitee_name (VARCHAR NOT NULL)                                │
│  start_time (TIMESTAMP NOT NULL)                                │
│  end_time (TIMESTAMP NOT NULL)                                  │
│  status (VARCHAR NOT NULL)  ──▶ confirmed, cancelled, rescheduled│
│  version (INTEGER DEFAULT 1)  ──▶ For optimistic locking       │
│  created_at (TIMESTAMP)                                         │
│                                                                 │
│  CONSTRAINT: Exclusion using GiST to prevent overlapping        │
│  bookings for same host (host_user_id, tsrange(start, end))    │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                  CALENDAR_INTEGRATIONS                           │
├─────────────────────────────────────────────────────────────────┤
│  id (UUID PK)                                                   │
│  user_id (UUID FK → users)                                      │
│  provider (VARCHAR NOT NULL)  ──▶ google, outlook               │
│  access_token (TEXT NOT NULL)  ──▶ Encrypted                   │
│  refresh_token (TEXT NOT NULL)  ──▶ Encrypted                  │
│  token_expires_at (TIMESTAMP NOT NULL)                          │
│  calendar_id (VARCHAR)                                          │
│  is_active (BOOLEAN DEFAULT true)                               │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🌍 Step 6: Time Zone Handling

"Time zones are surprisingly complex. Here's my approach:

**Principle: Store UTC, Display Local**

```
┌─────────────────────────────────────────────────────────────────┐
│                   TIME ZONE STRATEGY                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  STORAGE: All timestamps in UTC                                 │
│  ─────────────────────────────                                  │
│  bookings.start_time = '2024-01-15T19:00:00Z'                  │
│  No timezone ambiguity, no DST issues in storage               │
│                                                                 │
│  DISPLAY: Convert to user's timezone                           │
│  ────────────────────────────────                               │
│  For host (PST): "January 15, 2024 at 11:00 AM"                │
│  For invitee (EST): "January 15, 2024 at 2:00 PM"              │
│                                                                 │
│  AVAILABILITY RULES: Store in host's timezone                   │
│  ─────────────────────────────────────────────                  │
│  "9:00 AM - 5:00 PM" means host's local time                   │
│  Convert to UTC when calculating availability                  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**DST Edge Cases:**
- Store all times in UTC (no DST in UTC)
- Re-calculate display times on render (catches DST changes)
- For recurring meetings, store the rule (every Tuesday 2pm host's time), not the specific UTC time

**Confirmation Emails (Show Both Time Zones):**

```
┌─────────────────────────────────────────────────────────────────┐
│                    Meeting Scheduled!                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  For you (New York): Tuesday, Jan 15, 2024 at 2:00 PM EST      │
│  For your host (London): Tuesday, Jan 15, 2024 at 7:00 PM GMT  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```"

---

## 🔌 Step 7: API Design

### Availability API

```
┌─────────────────────────────────────────────────────────────────┐
│  GET /api/availability/slots                                     │
│      ?meeting_type_id=xxx                                       │
│      &date=2024-01-15                                           │
│      &timezone=America/New_York                                 │
├─────────────────────────────────────────────────────────────────┤
│  Response:                                                       │
│  {                                                              │
│    "date": "2024-01-15",                                        │
│    "timezone": "America/New_York",                              │
│    "slots": [                                                   │
│      {"start": "09:00", "end": "09:30"},                       │
│      {"start": "09:30", "end": "10:00"},                       │
│      {"start": "10:30", "end": "11:00"},                       │
│      ...gaps where host is busy...                              │
│    ]                                                            │
│  }                                                              │
└─────────────────────────────────────────────────────────────────┘
```

### Booking API

```
┌─────────────────────────────────────────────────────────────────┐
│  POST /api/bookings                                              │
├─────────────────────────────────────────────────────────────────┤
│  Request:                                                        │
│  {                                                              │
│    "meeting_type_id": "xxx",                                    │
│    "start_time": "2024-01-15T14:00:00Z",                       │
│    "invitee_name": "John Doe",                                  │
│    "invitee_email": "john@example.com",                         │
│    "invitee_timezone": "America/New_York"                       │
│  }                                                              │
├─────────────────────────────────────────────────────────────────┤
│  Response (201 Created):                                         │
│  {                                                              │
│    "id": "booking-uuid",                                        │
│    "status": "confirmed",                                       │
│    "start_time": "2024-01-15T14:00:00Z",                       │
│    "end_time": "2024-01-15T14:30:00Z",                         │
│    "calendar_event_id": "google-event-id"                       │
│  }                                                              │
├─────────────────────────────────────────────────────────────────┤
│  Response (409 Conflict):                                        │
│  {                                                              │
│    "error": "SLOT_UNAVAILABLE",                                 │
│    "message": "This slot was just booked",                      │
│    "alternatives": [...]                                        │
│  }                                                              │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔬 Step 8: Deep Dive - The Booking Race Condition

"Let me walk through the most challenging scenario: two people trying to book the last available slot simultaneously.

**Scenario:**
- Host has one slot available: 2:00-2:30 PM
- Alice and Bob both click 'Book' at the same time

**Without proper handling:**

```
┌─────────────────────────────────────────────────────────────────┐
│                    RACE CONDITION (BAD)                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Time T0: Alice checks availability ──▶ slot available         │
│  Time T1: Bob checks availability ──▶ slot available           │
│  Time T2: Alice creates booking ──▶ success                    │
│  Time T3: Bob creates booking ──▶ DOUBLE BOOKING!              │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**With my solution:**

```
┌─────────────────────────────────────────────────────────────────┐
│                  SERIALIZED BOOKING (GOOD)                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Time T0: Alice checks availability ──▶ slot available         │
│  Time T1: Bob checks availability ──▶ slot available           │
│  Time T2: Alice starts transaction, locks host row             │
│  Time T3: Bob starts transaction, WAITS for lock               │
│  Time T4: Alice inserts booking, commits, releases lock        │
│  Time T5: Bob acquires lock, checks availability ──▶ CONFLICT  │
│  Time T6: Bob gets error: 'Slot no longer available'           │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**User Experience for Bob:**

```
┌─────────────────────────────────────────────────────────────────┐
│  Sorry, this slot was just booked. Please select another time.  │
│                                                                 │
│  [Show updated availability with slot removed]                  │
│  [Suggest next available slot]                                  │
└─────────────────────────────────────────────────────────────────┘
```

> "The key insight is that we serialize booking attempts at the database level using row locking. This adds ~10-50ms latency but guarantees correctness. For a scheduling product, users will accept slightly slower bookings over double-booking disasters."

---

## 📅 Step 9: Calendar Integration Deep Dive

### OAuth Flow

```
┌─────────┐     ┌─────────────┐     ┌─────────────┐
│  User   │────▶│ Calendly    │────▶│   Google    │
│ Browser │     │   Backend   │     │   OAuth     │
└─────────┘     └─────────────┘     └─────────────┘
     │                 │                   │
     │  1. Click       │                   │
     │  'Connect       │                   │
     │   Google'       │                   │
     │────────────────▶│                   │
     │                 │ 2. Redirect to    │
     │                 │    Google OAuth   │
     │◀────────────────│───────────────────│
     │                 │                   │
     │  3. User logs   │                   │
     │  in to Google   │                   │
     │─────────────────│──────────────────▶│
     │                 │                   │
     │  4. Google      │                   │
     │  redirects      │                   │
     │  with code      │                   │
     │◀────────────────│───────────────────│
     │                 │                   │
     │  5. Exchange    │                   │
     │  code for       │  6. Access +      │
     │  tokens         │  Refresh tokens   │
     │                 │◀──────────────────│
```

### Token Management

```
┌─────────────────────────────────────────────────────────────────┐
│                   TOKEN REFRESH FLOW                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  getValidToken(integration_id):                                 │
│  ├── Load integration from database                            │
│  ├── If token_expires_at > now ──▶ Return access_token         │
│  └── Else (expired):                                           │
│      ├── Call Google OAuth refresh endpoint                    │
│      ├── Update database with new access_token, expires_at     │
│      └── Return new access_token                               │
│                                                                 │
│  Error handling:                                                │
│  ├── RefreshTokenInvalid ──▶ Mark integration as disconnected  │
│  └── NetworkError ──▶ Retry with exponential backoff           │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### What if Calendar API is Down?

```
┌─────────────────────────────────────────────────────────────────┐
│                CALENDAR API FAILURE HANDLING                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  create_booking_with_calendar(booking_data):                    │
│                                                                 │
│  1. Create booking in our database FIRST                        │
│     └── This is the source of truth                            │
│                                                                 │
│  2. Try to create calendar event                                │
│     ├── SUCCESS ──▶ Store calendar_event_id on booking         │
│     └── FAILURE ──▶ Queue for retry, don't fail the booking    │
│                                                                 │
│  3. Return booking confirmation to user                         │
│     └── "Meeting confirmed! Calendar invite may take a moment" │
│                                                                 │
│  Background job retries failed calendar creations               │
│  every 5 minutes with exponential backoff                       │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

> "We prioritize booking creation over calendar sync. Users might not see the event immediately, but the booking is confirmed. This is the right trade-off because missing a calendar invite is recoverable; losing a booking is not."

---

## 📈 Step 10: Scalability Considerations

### Database Scaling

"Bookings table is the main growth concern:

**Partitioning by Date:**

```
┌─────────────────────────────────────────────────────────────────┐
│                   MONTHLY PARTITIONING                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  bookings (parent table)                                        │
│  PARTITION BY RANGE (start_time)                                │
│      │                                                          │
│      ├──▶ bookings_2024_01 (Jan 1 - Feb 1)                     │
│      ├──▶ bookings_2024_02 (Feb 1 - Mar 1)                     │
│      ├──▶ bookings_2024_03 (Mar 1 - Apr 1)                     │
│      └──▶ ...                                                   │
│                                                                 │
│  Benefits:                                                      │
│  - Old partitions can be archived to cold storage               │
│  - Queries scoped to date range only scan relevant partitions   │
│  - Easy to drop old data without full table locks               │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**Read Replicas:**
- Availability queries hit read replicas (500-5000 RPS)
- Booking creation hits primary (5-50 RPS)
- Replication lag acceptable (few ms) for availability checks"

### Caching Strategy

```
┌─────────────────────────────────────────────────────────────────┐
│                     CACHE LAYERS                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  1. Availability Cache (Redis)                                  │
│     Key: availability:{host}:{date}:{type}                      │
│     TTL: 5 minutes                                              │
│     Invalidate: On booking, On calendar sync                    │
│                                                                 │
│  2. Calendar Events Cache (Redis)                               │
│     Key: calendar:{integration_id}:{date}                       │
│     TTL: 10 minutes                                             │
│     Invalidate: On calendar webhook, On sync                    │
│                                                                 │
│  3. User/Meeting Type Cache (Redis)                             │
│     Key: user:{id}, meeting_type:{id}                           │
│     TTL: 1 hour                                                 │
│     Invalidate: On update                                       │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📧 Step 11: Notification System

```
                    ┌─────────────────┐
                    │  Booking Event  │
                    └────────┬────────┘
                             │
                             ▼
                    ┌─────────────────┐
                    │    RabbitMQ     │
                    │   (Job Queue)   │
                    └────────┬────────┘
                             │
           ┌─────────────────┼─────────────────┐
           ▼                 ▼                 ▼
    ┌─────────────┐   ┌─────────────┐   ┌─────────────┐
    │Confirmation │   │  Reminder   │   │Cancellation │
    │   Worker    │   │   Worker    │   │   Worker    │
    └─────────────┘   └─────────────┘   └─────────────┘
           │                 │                 │
           ▼                 ▼                 ▼
    ┌─────────────────────────────────────────────────┐
    │               Email Service (SendGrid)           │
    └─────────────────────────────────────────────────┘
```

**Reminder Scheduling:**
- On booking creation, schedule two delayed messages:
  - 24 hours before meeting: "Reminder: Meeting tomorrow"
  - 1 hour before meeting: "Reminder: Meeting in 1 hour"
- If booking is cancelled, remove scheduled reminders from queue

---

## ⚖️ Step 12: Trade-offs and Alternatives

| Approach | Pros | Cons |
|----------|------|------|
| ✅ PostgreSQL | ACID guarantees, exclusion constraints for overlap prevention, mature tooling | Single-node write bottleneck at extreme scale |
| ❌ DynamoDB | Infinite horizontal scale, managed service | No native overlap prevention, eventual consistency complicates booking |
| ✅ Pessimistic Locking (FOR UPDATE) | Guarantees no double bookings, simple mental model | Adds 10-50ms latency, potential for lock contention |
| ❌ Optimistic Only | Lower latency for happy path | More complex retry logic, higher conflict rate |
| ✅ UTC Storage | No DST issues, single source of truth | Requires conversion for every display |
| ❌ Local Time Storage | Simpler display logic | DST hell, timezone changes break existing data |
| ✅ Hybrid Calendar Sync (webhook + polling) | Real-time when webhooks work, polling catches failures | More complex implementation |
| ❌ Polling Only | Simpler implementation | 10-minute staleness, wastes API quota |
| ✅ 5-min Availability Cache | Reduces load 80%+, good enough freshness | Stale data possible, requires invalidation logic |
| ❌ No Cache | Always fresh | 5000 RPS would overwhelm database |

---

## 📊 Step 13: Monitoring

"Key metrics to track:

**Business Metrics:**
- Bookings per minute
- Booking success rate (vs. conflicts)
- Availability check latency (p50, p95, p99)

**Technical Metrics:**
- Database lock wait time
- Cache hit rate for availability (target: >80%)
- Calendar API error rate
- Email delivery rate

**Critical Alerts:**
- Double booking detected (should be 0, indicates bug)
- Availability check latency > 500ms
- Calendar sync failure rate > 5%
- Email bounce rate > 2%"

---

## 🎯 Summary

"To summarize my Calendly design:

1. **Double Booking Prevention**: Multi-layered approach with database constraints, row locking, and optimistic locking
2. **Availability Calculation**: Merge working hours, bookings, and calendar events with smart caching (5-min TTL)
3. **Time Zone Handling**: Store UTC, display local, handle DST on render
4. **Calendar Integration**: OAuth with token refresh, hybrid sync (webhooks + polling)
5. **Scalability**: Read replicas, date-range partitioning, aggressive caching

> "The key insight is that consistency (no double bookings) trumps availability - we'd rather fail a booking attempt than create a conflict. The system is designed around this principle."

What aspects would you like me to elaborate on?"
